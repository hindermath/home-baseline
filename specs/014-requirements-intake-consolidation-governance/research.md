# Research Decisions

1. Extend three presets; a fourth would duplicate authoring, review, and
   sequencing policy.
2. Make BCP-47 configuration authoritative. Automatic detection remains
   advisory and may not authorize writes.
3. Use stable role IDs before localized names.
4. Require a hash-bound operations journal for multi-file migration.
5. Reuse three prior field migrations as regressions and four different
   repositories as adoption tests without promoting their project counts.
