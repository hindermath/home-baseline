# Candidate Quickstart

This feature produces an unreleased candidate. During development, run the
new validators directly from the canonical preset tree, then run the matching
PowerShell command. Use temporary repositories for delivery-set tests and
temporary evidence paths for exact-head snapshots.

Do not install this candidate into consumer repositories or publish a tag.
G2B performs independent installation, field validation, release, and rollout.
