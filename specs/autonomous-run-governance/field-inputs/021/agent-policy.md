# Causal Remote Evidence Field Policy

Feature 021 confirmed the same synchronized policy across `AGENTS.md`,
`CLAUDE.md`, `GEMINI.md`, `.github/copilot-instructions.md`, and
`.github/agents/copilot-instructions.md` at TuiVision main commit
`0ed394c4b0e6a96b3eaa5b582d88b9e96401c040`.

> Aktuelle Check-/Review-Fakten werden vor dem Merge geprüft, aber in genau
> einen benannten Closeout-Evidence-Pfad verschoben, wenn ihr Commit den
> geprüften Feature-Head und damit die Aussage selbst entwerten würde.

> Route self-invalidating reviewed-head facts to one named closeout path after
> verifying them before merge.

The 021 field run pre-named the closeout path before delivery, verified the
feature head without changing it, and recorded reviewed-head plus post-merge
facts afterward. Portable preset work must preserve the invariant without
granting remote write, merge, or bypass authority.
