# Research: Feature 013

- The check-only maintenance preflight found 44 remote-current desired targets.
- The canonical registry contains eight Level-1 and 24 Level-2 entries; the
  broader audit also covers 12 separately registered Level-2 learning targets,
  for 36 Level-2 outcomes in total.
- Thirty-two registry-controlled repositories have the same expected D2
  maintenance-package drift: 416 file differences.
- All eleven preset profiles resolve exactly.
- D1 produced 15 Medium bilingual findings. The current D2 checker must
  re-evaluate them before any content is added because existing DE/EN markers
  may already satisfy the corrected contract.
- Local Home runtime drift is outside repository delivery and is handled by
  the normal post-merge source-sync boundary.
